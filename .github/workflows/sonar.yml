name: SonarQube Analysis

on:
  workflow_call:
    inputs:
      project-key:
        description: 'SonarQube project key (default: repo name)'
        required: false
        type: string
        default: ''
      project-name:
        description: 'SonarQube project name (default: project-key)'
        required: false
        type: string
        default: ''
      sonar-host-url:
        description: 'SonarQube server URL'
        required: false
        type: string
        default: 'https://ellarea-int.innov-ds.com/sonarqube'
      java-version:
        description: 'Java version for analysis'
        required: false
        type: string
        default: '21'
      sources:
        description: 'Source directories'
        required: false
        type: string
        default: 'src/main/java'
      tests:
        description: 'Test source directories'
        required: false
        type: string
        default: 'src/test/java'
      exclusions:
        description: 'Files to exclude from analysis'
        required: false
        type: string
        default: '**/test/**,**/target/**'
      skip-tests:
        description: 'Skip tests (no coverage)'
        required: false
        type: boolean
        default: false

    secrets:
      SONAR_TOKEN:
        required: true
      MAVEN_SETTINGS_XML:
        required: false

permissions:
  contents: read

jobs:
  sonar:
    name: SonarQube Analysis
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ inputs.java-version }}

      - name: Maven settings
        uses: innovds/ids-workflows/actions/maven-settings@main
        id: maven
        with:
          settings-base64: ${{ secrets.MAVEN_SETTINGS_XML }}

      - name: Build and Test with Coverage
        if: ${{ !inputs.skip-tests }}
        run: |
          ./mvnw verify --settings ${{ steps.maven.outputs.settings-path }} \
            -Dtest='!LiquibaseValidationTest'

      - name: Build only (no tests)
        if: ${{ inputs.skip-tests }}
        run: ./mvnw compile -DskipTests --settings ${{ steps.maven.outputs.settings-path }}

      - name: Copy dependencies
        run: ./mvnw dependency:copy-dependencies -DoutputDirectory=target/dependency --settings ${{ steps.maven.outputs.settings-path }}

      - name: Resolve project
        id: project
        run: |
          KEY="${{ inputs.project-key }}"
          [ -z "$KEY" ] && KEY="${{ github.event.repository.name }}"
          echo "key=$KEY" >> $GITHUB_OUTPUT

          NAME="${{ inputs.project-name }}"
          [ -z "$NAME" ] && NAME="$KEY"
          echo "name=$NAME" >> $GITHUB_OUTPUT

      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v3
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ inputs.sonar-host-url }}
        with:
          args: >
            -Dsonar.projectKey=${{ steps.project.outputs.key }}
            -Dsonar.projectName=${{ steps.project.outputs.name }}
            -Dsonar.sources=${{ inputs.sources }}
            -Dsonar.tests=${{ inputs.tests }}
            -Dsonar.exclusions=${{ inputs.exclusions }}
            -Dsonar.java.binaries=target/classes
            -Dsonar.java.libraries=target/dependency/*.jar
            -Dsonar.java.test.binaries=target/test-classes
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml
            -Dsonar.sourceEncoding=UTF-8
